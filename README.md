# ReadEase

Web-based PDF reader with an instant translation pop-up.

## Dev

- Install: `npm install`
- Run: `npm run dev`

## Environment (optional)

- `LIBRETRANSLATE_URL` (default: `https://libretranslate.de/translate`)

## Deploy

This project deploys cleanly on Vercel (Next.js default settings).
